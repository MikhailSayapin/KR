#include <iostream>
#include <string>
#include <cstring>
#include <limits>
#include <stdlib.h>

using namespace std;

int main()
{
    string razm, str1, str2;
    int a;
    a = 0;
    cin >> razm;
    cin.ignore(numeric_limits<streamsize>::max(), '\n');
    getline(cin, str1);
    for (int i = 0; i < str1.length(); i++)
    {
        if (str1[i] >= '0' && str1[i] <= '9')
            ;
        else
            a++;
    }
    a++;
    if (atoi(razm.c_str()) == a)
    {

        for (int i = 0, j = 0; i < str1.length(); i++)
            if (str1[i] >= '0' && str1[i] <= '9')
                str2[j] = str2[j] + str1[i];
            else
                j++;
        for (int i = 0; i < a / 2; i++)
            swap(str2[i], str2[a - i - 1]);
        str1 = "";
        for (int i = 0; i < a; i++)
            str1 = str1 + str2[i] + " ";
        cout << str1 << endl;
    }
    else
        cout << "An error has occurred while reading input data" << endl;

    return 0;
}
